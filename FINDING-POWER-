#include <stdio.h>
#include <math.h>

int main()
{
    int x; 
    int y;
    int ans;
    printf("enter base : ");
    scanf("%d",&x);
    printf("enter power : ");
    scanf("%d",&y);
    ans = pow(x, y);
    printf("%d", ans);   
}
