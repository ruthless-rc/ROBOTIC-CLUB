#include <stdio.h>
#include <stdio.h>

int main()
{
    int i,j,k,l=1;

    for (i = 0; i < 5; i++) {

        for (j = 0; j < (5 - i) - 1; j++) {
            printf(" ");
        }

        for (k = 1; k <= i + 1; k++) {
            printf("%d ",k);
        }
        printf("\n");
    }
}
